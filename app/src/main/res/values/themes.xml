<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Click_N_Find" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="backgroundImage">@raw/day_mode_background</item>
        <item name="colorPrimary">@color/lightModeMainColor</item>
        <item name="android:textColor">@color/lightModeTextColor</item>
    </style>

    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/lightModeTextColor</item>
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Click_N_Find" parent="Base.Theme.Click_N_Find" />
</resources>